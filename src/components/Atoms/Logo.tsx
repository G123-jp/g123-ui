import React from 'react';

import LogoImg from './images/G123_logo.svg';

const Logo: React.FC = () => {
  return <img alt='' src={LogoImg} style={{ width: '42px', height: '14px' }} />;
};

export default Logo;
