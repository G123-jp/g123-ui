import tailwindConfig from './tailwind.config.js';

// Modify the darkMode configuration
tailwindConfig.darkMode = 'class'; // or 'media' or 'false'

// Export the modified configuration
export default tailwindConfig;
